-- Select all columns (*) from the table "city"
SELECT * 
-- From the table "city" (assumes a table named "city" exists in the database)
FROM city 
-- Filter the results to include only rows where the "id" column equals 1661
WHERE id = 1661;
